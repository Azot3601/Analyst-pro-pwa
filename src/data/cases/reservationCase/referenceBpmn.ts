import type { BpmnReference } from '../../../shared/lib/graphCheckers';

// Эталон BPMN кейса «Бронирование столиков». Проверяем структуру, а не форму:
// нужные типы узлов, достижимость конца и обязательная ветка-исключение (неявка).

export const reservationBpmn: BpmnReference = {
  task:
    'Смоделируй процесс бронирования столика. Обязательно учти исключение: что происходит ' +
    'при неявке гостя (no-show) — по этой ветке выставляется штраф. Процесс должен иметь старт, ' +
    'задачи, исключающий шлюз с условием про неявку и завершаться end-событием на каждом пути.',
  requiredKinds: ['start', 'end', 'task', 'gateway-x'],
  exceptionKeywords: ['неяв', 'не приш', 'no_show', 'no-show']
};
